name: Deploy
on: [push, workflow_dispatch]

jobs:
    clean: 
        name: Clean
        runs-on: ubuntu-latest
        steps:
          - name: executing remote ssh commands using password
            uses: appleboy/ssh-action@v1.1.0
            with:
                host: ${{ secrets.HOST }}
                username: ${{ secrets.USERNAME }}
                key: ${{ secrets.KEY }}
                script: |
                    rm -rf /var/www/html/*
                    echo "Cleaned web directory"
    build:
        name: Build
        runs-on: ubuntu-latest
        needs: clean
        steps:
          - name: executing remote ssh commands using password
            uses: appleboy/ssh-action@v1.1.0
            with:
                host: ${{ secrets.HOST }}
                username: ${{ secrets.USERNAME }}
                key: ${{ secrets.KEY }}
                script: |
                    cd ~
                    git clone https://github.com/bendevnull/ootmm-shared-tracker.git
                    cd ootmm-shared-tracker
                    npm install
                    npm run build
                    echo "Build completed"
    deploy:
        name: Deploy
        runs-on: ubuntu-latest
        needs: build
        steps:
          - name: executing remote ssh commands using password
            uses: appleboy/ssh-action@v1.1.0
            with:
                host: ${{ secrets.HOST }}
                username: ${{ secrets.USERNAME }}
                key: ${{ secrets.KEY }}
                script: |
                    cd ~
                    cp -r ootmm-shared-tracker/build/* /var/www/html/
                    cp ootmm-shared-tracker/server.js /opt/tracker-server/
                    pm2 restart tracker-server
                    echo "Deployed"
            